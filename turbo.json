{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**"]
    },
    "e2e": {
      "outputs": ["test-results/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
